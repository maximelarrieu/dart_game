extends skeleton.pug 

block content 
    h1 Liste des joueurs de la partie 

    if players.length === 0
       p Pas de parties trouvées
    else
        table(style="width:50%", border=5)
            tr
                th Nom 
                th Email 
                th Actions
            each player in players 
                tr(style="text-align: center") 
                    td= player.name 
                    td= player.email
                    td
                        form(action=`/api/games/${id}/players/${player.id}`, method="POST")
                            input(type="submit" value="TEST")
                        //- form(action=`/api/games/${game.id}/delete`, method="POST")
                        //-     input(type="submit" value="Supprimer")